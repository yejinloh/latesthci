class Product {
  String code, name, brand, type, color, size, price, quantity, sold;

  Product({
    this.code,
    this.name,
    this.brand,
    this.type,
    this.color,
    this.size,
    this.price,
    this.quantity,
    this.sold,
  });
}
